{
  "name": "seans_pub",
  "version": "1.0.0",
  "description": "Personal Website",
  "main": "index.js",
  "scripts": {
    "fetch": "node index.js fetch",
    "build": "node index.js build",
    "copy": "cp -a ./dest/. /var/www/aggregator/public_html/",
    "deploy": "npm run fetch && npm run build && npm run copy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sean",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "handlebars": "^4.3.0",
    "moment": "^2.24.0"
  }
}
